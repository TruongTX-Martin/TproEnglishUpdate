import Constants from '../Config/Constant';
const Transcript = {
  name: Constants.TRANSCRIPT,
  properties: {
    name: { type: Constants.STRING },
    content: { type: Constants.STRING }
  }
};

export default Transcript;
